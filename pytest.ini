[pytest]
# General pytest configuration
# Note: Playwright-specific options should be passed via command line
# Example: pytest test_search_playwright.py --headed --slowmo=1000 --browser=chromium

# For Playwright tests, use:
# pytest test_search_playwright.py -v --headed --slowmo=1000 --browser=chromium

# For Selenium tests, use:
# pytest python-tests/test_search_pytest.py -v
